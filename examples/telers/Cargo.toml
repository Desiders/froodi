[package]
name = "telers"
version = "1.0.0"
edition = "2024"

[dependencies]
froodi = { path = "../../froodi", features = ["telers"] }
telers = { version = ">=1.0.0-beta.1, <1.0.0" }
tokio = { version = "1.36", features = ["rt", "macros"] }
